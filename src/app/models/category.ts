export interface Category{
    category_name: string;
    parent_category_id: number;
    description: string;
    display_order: number;
    image_url: string;
    is_active: boolean;
    category_id: number;
}